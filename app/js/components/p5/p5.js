const s = ( sketch ) => {

    sketch.setup = () => {
        sketch.createCanvas(sketch.windowWidth, sketch.windowHeight/2);
    };

    sketch.draw = () => {
        sketch.background(51);

        


    };
};

export default s;
